/**
 * @brief finds maximum number between two numbers
 * @param a: first number
 * @param b: second number
 * @return maximum between a and b
 */
pub func max(a: usize, b: usize): usize {
    return a >= b ? a : b;
}
